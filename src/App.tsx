import { GameContainer } from './components/GameContainer';

export default function App() {
  return (
    <div className="w-screen h-screen overflow-hidden bg-[#0A0A1A] flex items-center justify-center">
      <GameContainer />
    </div>
  );
}
